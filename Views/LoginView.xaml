<Window x:Class="Kalum2020v1.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalum2020v1"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" Name="FormularioLogin">

    <Border Background="#E0F2F7" CornerRadius="20" Margin="20">
        <StackPanel Margin="20">
            <Label Foreground="Black" FontSize="25" HorizontalAlignment="Center">
                <Bold>Login</Bold>
            </Label>
            <Separator Background="Black"/>
            <Label Foreground="Blue">
                <Bold>Username: </Bold>
            </Label>
            <TextBox Name="txtUsername" Text="{Binding Username}" 
                Foreground="Black"
                FontSize="18"/>
            <Label Foreground="Blue">
                <Bold>Password : </Bold>
            </Label>
            <PasswordBox x:Name="txtPassword" Foreground="Black" FontSize="18"/>
            <Button Content="Entrar"  Margin="60 10" 
                Command="{Binding Instancia}" CommandParameter="{Binding ElementName=FormularioLogin}"
                Background="#1979A9"
                Foreground="Black" Height="50" />
        </StackPanel>
    </Border>

</Window>        